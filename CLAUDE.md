# Claude Code — Agent Config

⚠️ **STOP. Read `../AGENT.md` first** (one level up from app/).
All architecture rules are defined there.

This file is for Claude Code running inside the `app/` directory.
See root `CLAUDE.md` for full platform-specific details.
