import { meta } from './meta'
import { en } from './en'
import { ru } from './ru'
import type { BlogData } from '../../_lib/post'

// Сборка записи: непереводимая мета + английская основа + переводы по языкам.
// Резолвер (`lib/content/resolve.ts`) склеивает их ПОКЛЮЧЕВО, поэтому язык,
// переведённый наполовину, получает свои поля переведёнными, а остальные — из
// основы.
//
// 🔒 ЯЧЕЕК РОВНО СТОЛЬКО, СКОЛЬКО ЯЗЫКОВ ВКЛЮЧЕНО. Набор задаёт
// `NEXT_PUBLIC_SUPPORTED_LANGUAGES` в `.env.local`; сегодня это `en,ru`, и
// потому здесь две ячейки, а не десять «как у соседей». Включённый язык без
// ячейки — это не запись поменьше: это английский текст по чужому адресу,
// объявляющий себя переводом. За этим следит `check:content`
// (`translation-coverage`).
export const data: BlogData = { meta, en, overrides: { ru } }
