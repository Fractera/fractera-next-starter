import type { BlogMeta } from '../../_lib/types'

// Непереводимые поля записи: slug, дата, теги и картинка — одни на все языки.
//
// 🔒 АВТОРА ЗДЕСЬ НЕТ НАМЕРЕННО. Подпись берётся из настроек проекта
// (`lib/author.ts` → панель, App settings → Author). Поле `author` остаётся для
// честного случая — гостевой статьи, написанной не владельцем сайта.
//
// 🔒 `heroVideo` НЕТ, И ЭТО ЗАКОННОЕ СОСТОЯНИЕ, А НЕ НЕДОДЕЛКА. Обложка-видео
// рисуется только при наличии файла видео (`_lib/post.tsx`), поэтому шапки у
// записи не будет вовсе. Картинка при этом работает в двух местах: `heroPoster`
// рисует карточку в списке блога, `ogImage` уезжает в карточку соцсети и в
// структурированные данные. В самой статье она стоит блоком `figure` — там у
// неё переводимые подпись и альтернативный текст, чего у поля меты быть не может.
//
// 🔒 ВНУТРИ КАРТИНКИ НЕТ НИ ОДНОГО СЛОВА. Сайт двуязычный, а вписанное в пиксели
// слово переводом не становится — тот же довод записан в мете страницы «О нас».
// Смысл несут фигуры: полка уже отпечатанных страниц и окно браузера, которому
// одну из них выдали целиком.
export const meta: BlogMeta = {
  slug: 'why-our-pages-are-static',
  date: '2026-08-21',
  readingMinutes: 7,
  tags: ['Static pages', 'Web performance', 'How we build'],
  heroPoster: '/blog-media/pages-built-in-advance.jpg',
  ogImage: '/blog-media/pages-built-in-advance.jpg',
}
