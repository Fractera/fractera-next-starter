import { AiCoreApp } from "./ai-core-app.client"

// Route entry component for /ai-core. Server by default; renders the interactive
// AI-core explorer island.
export default function AiCoreEntry() {
  return <AiCoreApp />
}
