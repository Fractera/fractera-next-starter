# BACKLOG — what is built but unexplained, half-built, or owed

**This is not instruction.** `CLAUDE.md` holds the laws and is read at the start of every session;
this file is the list of **your own** unfinished work and is read when the area it names comes up. It
left the instruction on 2026-08-23 for exactly that reason: a backlog loaded into every session is
paid for by every session, including the ones it has nothing to do with.

**How an entry leaves:** the work is done and the law moves into `CLAUDE.md` or a skill, and the line
is deleted here. An empty file means nothing is owed.

🔒 **This file arrives EMPTY in a new project, and that is a law (2026-08-24).** It holds the debts of
THIS project. The debts of whoever built the starter are a foreign history — they name files you do
not have and decisions you never took — and they used to travel here to every client. They now live in
the Fractera dev repository (`code/development-docs/BACKLOG.md`).

**Form of an entry:** a `## 🔶` heading naming the thing in one line · what works and what proves it ·
what is missing · what it would cost to finish. An entry without "what is missing" is not a debt, it
is a note.

---

*Nothing is owed yet.*
