# 🪦 CASE-TO-STEP.md — переехал в навык 2026-08-23

**Здесь лежали 92 строки о пути «кейс → шаг».** Живое перенесено в навык
[`use-use-cases`](../.claude/skills/use-use-cases/SKILL.md); документ снят, и не только из-за
дублирования.

🔒 **Он врал в двух местах, и обе неправды были структурные — по ним строили бы.**

| Что утверждал документ | Как на самом деле (проверено 2026-08-23) |
|---|---|
| три поверхности одного закона: **MCP `fractera-project`**, навык `manage-cases-and-steps`, этот файл | ни MCP, ни навыка с таким именем в проекте НЕТ — оба упоминаются только в самом документе. Из трёх поверхностей существовала одна: он сам |
| кейсы — файлы в `development-docs/USE-CASES/<product>/CASES/` | кейсы живут в досье продукта: `PRODUCTS-CONFIG/<id>.json`, массив `cases[]` (`slug`, `title`, `summary`, `confirmed`, …). Папки `USE-CASES/` в проекте нет |

**Что было правдой и переехало:** два входа к шагу разбора (панель и агент, оба идемпотентны), поиск
шага разбора по `kind`, а не по заголовку; сквозной и вечный номер шага; `platform` как законное
значение `product_id`; подтверждает кейс только владелец; правка подтверждённого кейса возвращает его
в черновик; заголовок шага 6–12 слов; этап двигается только вперёд; первый шаг продукта — работающий
каркас.

**Рассуждение «почему навык, а не инструкция»** (цена контекста, правила конкурируют за внимание,
копии расходятся, закон нужен в минуту применения) не переехало в навык намеренно: это закон о том,
как устроен корпус, и он уже стоит в `development-docs/README.md` — «три адреса для трёх родов знания».

**Сам файл не удалён:** удаление в этой папке — решение владельца, а не агента (`README.md`).
Содержимое до переезда — в git.
