# 🪦 CONTENT-ENGINE.md — переехал в навыки 2026-08-23

**Здесь лежали 584 строки о том, как строится пост.** Всё живое из них перенесено; документа больше
нет, потому что второй источник тех же правил расходится с первым — и расходится в том месте, куда
никто не смотрит.

**Почему нельзя было оставить как есть.** Документ объявлял **две** модели страницы, а их **три**:
публичной страницы ИЗ ДАННЫХ (каталог, `[slug]` + `generateStaticParams` на срез + порционная карта
сайта) в нём не было вовсе. Его §2 отвечал на вопрос «папка или динамический сегмент» — и для
каталога отвечал неверно.

| Что было | Куда переехало |
|---|---|
| движок, четыре слоя, рецепт поста, языковые ячейки, закон двух ссылок, идентичность из настроек, гейт `check:content`, новая вкладка одной строкой в `COLLECTIONS`, закрытая пара фабрик | `use-static-pages` |
| пользовательская поверхность: динамический сегмент, данные за `/api/*`, статическая оболочка | `use-dynamic-pages` |
| картинки: `media:` по имени, семенной файл и `seed:media`, ловушки ссылки на картинке и постера видео | `use-object-storage` |
| карта сайта, порционная карта и предел в 50 000 адресов | `use-seo` |
| машинный близнец `index.md` | `use-aio` |
| каталог видов, образец на каждый вид, цвет только токеном | `use-sections` |
| два недоказуемых гейтом требования: `●` в таблице сборки и текст без JavaScript | `use-testing`, `use-static-pages` §8 |
| выбор модели страницы | `CLAUDE.md`, раздел «Три модели страницы» |

**Сам файл не удалён:** удаление в этой папке — решение владельца, а не агента (`README.md`).
Содержимое до переезда — в git.
