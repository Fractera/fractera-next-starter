# 🪦 DYNAMIC-WORKFLOWS.md — переехал в навык 2026-08-23

**Здесь лежали 145 строк процедуры** — как владелец включает волны агентов и чем за них платит.
Процедура нужна по конкретному поводу, а не всегда, поэтому её место навык, а не документ: она
приходит в минуту, когда за неё берутся, и до этого не занимает контекст.

**Переехало целиком в [`use-dynamic-workflows`](../.claude/skills/use-dynamic-workflows/SKILL.md):**
где это физически выполняется (агенты живут в ТОЙ ЖЕ сессии и на той же машине, `acceptEdits` у всех,
входа внутрь запуска нет), правило охраны («волна оправдана только там, где результат каждой единицы
судит машина»), способ отказа — не ошибка, а правдоподобный ответ в масштабе (случай `__UK_BLOCK__`),
цена и проверка по своей же прошлой неделе, два замка на двери, и когда выключать обратно.

**Закон «работаешь один» здесь никогда не жил** — он в `CLAUDE.md`, раздел «How we build», и остаётся
там: закон действует всегда, а поводом не вызывается.

**Сам файл не удалён:** удаление в этой папке — решение владельца, а не агента (`README.md`).
Содержимое до переезда — в git.
