# m06 — Привычки

**Кейс.** `02-client-keeps-habits`. **Зависит от** m02.

**Что делаем.** `p1_activities`: привычка внутри сферы — имя, значок, сфера. Страница
`balance/activities`, логика `lib/products/p1/activities.ts`.

**Доказательство.** Привычка создаётся внутри сферы и видна на её странице после перезагрузки.
