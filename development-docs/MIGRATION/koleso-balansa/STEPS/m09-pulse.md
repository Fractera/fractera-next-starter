# m09 — Пульс дня и статистика активностей

**Кейс.** `02-client-keeps-habits`. **Зависит от** m08.

**Что делаем.** Две страницы: `balance/statistics/pulse` и `balance/statistics/activities` — ряды из
`p1_activity_marks` с разбивкой по сферам.

**Доказательство.** Числа на обеих страницах сходятся с прямым запросом к слою данных за тот же период.
