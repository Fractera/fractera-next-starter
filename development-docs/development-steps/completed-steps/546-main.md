# 546 — Инструкция приведена в соответствие и пересобрана

**Три работы одним шагом:** регистрация навыков, вердикт по планам, хирургия документов и пересборка
главной инструкции.

## Регистрация и вердикт

Зарегистрированы четыре навыка: `use-object-storage`, `use-channels`, `use-products-config`,
`use-auth-providers`. По шести именам, стоявшим планом, вынесен вердикт: `use-troubleshooting`,
`use-passport`, `use-panel` **отменены** (их содержимое уже лежит в соседних навыках; имена убраны и из
таблицы навыков, и из таблицы портов); `use-dynamic-workflows` и `use-use-cases` **написаны в тот же
день**; `use-migration` остаётся планом с условием — сначала пройти два первых шага очереди на чужом
проекте.

## Хирургия трёх документов — переездом, а не удалением

| Документ | Куда переехал |
|---|---|
| `CONTENT-ENGINE.md` (584 строки) | разошёлся по семи навыкам |
| `DYNAMIC-WORKFLOWS.md` (145) | навык `use-dynamic-workflows` |
| `CASE-TO-STEP.md` (92) | навык `use-use-cases` |

На месте каждого — надгробие с картой переезда: удаление в `development-docs/` решает владелец.

🔒 **`CASE-TO-STEP.md` врал структурно.** Он объявлял три поверхности одного закона — MCP
`fractera-project` и навык `manage-cases-and-steps` не существуют вовсе — и селил кейсы в папке
`USE-CASES/`, тогда как они живут в досье продукта (`PRODUCTS-CONFIG/<id>.json`, массив `cases[]`).

## Пересборка `CLAUDE.md`: 1375 → 856 строк, по-английски

Не перевод, а переопределение. «Кто ты» стал первым блоком, доверие и граница выдумки вошли внутрь
него. Вместо прозы «чем оплачено» — нотация `🔒 закон · ✗ дефект · → навык · 🪦 отменено`. Backlog
вынесен в `development-docs/BACKLOG.md`, надгробия собраны в таблицу `Cancelled`.

**Сверка по реестру законов:** было 58 под 🔒, стало 52, каждая разница названа — два закона про
`/compact` слиты, три уехали в backlog, два в навыки, вердикт стал таблицей, один добавлен.
Непристроенных нет.

**Ошибки шага.** Проверка путей нашла третью выдумку за день: `PAGES.md` не существует — план страниц
живёт массивом `pages` в досье. Там же исправлены `registry.json` (создаётся в рантайме) и число
гейтов: восемнадцать в `prebuild`, а не пятнадцать. `app/AGENTS.md` пришлось переписать целиком: он
описывал `app/[lang]/legal/`, `app/api/legal/`, `app/(service)/` и `app/[lang]/page.tsx` — ни одного из
них в проекте нет.
