# current-steps.md — живое состояние работы

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается: здесь не хроника, а ответ на
вопрос «где я сейчас». Всё, что уже стало итогом, отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно** (решение владельца 2026-08-22): текущая работа почти никогда
не один шаг. Это группа связанных шагов и подшагов, и половина смысла файла — держать их связи и
условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале сессии**, раньше плана шага: план говорит, что задумано, этот файл — где
работа оборвалась.

---

## Сейчас в работе

**Группа:** память проекта учится переживать обрыв сессии · **начата** 2026-08-22

| Шаг | Что | Состояние | Начат | Закрывается, когда |
|---|---|---|---|---|
| 534 | `current-steps.md` и закон репорта фичи | в работе | 2026-08-22 | форма записана, доставлена на сервер, владелец посмотрел |
| 535 | навык `use-development-steps` | не начат | — | 534 закрыт — иначе навык опишет замысел вместо сущности |

**Попутные шаги** — те, которых в проектировании не было, а делать пришлось. Записывать сразу и с
причиной: без них цена фичи в итоговом репорте окажется заниженной, и следующая оценка будет врать.

| Шаг | Почему появился |
|---|---|
| — | пока нет |

**Условие закрытия группы.** 534 закрывается первым и один; 535 не начинать раньше — навык о режиме
`steps` обязан ссылаться на готовую сущность, а не на план.

**Сделано и чем доказано.**
- Сущность заведена: этот файл; форма — ниже, в разделе «Форма записи».
- Закон репорта фичи записан в `CLAUDE.md`, раздел «Твоя память»: повод, содержимое, три роли, имя с
  категорией `feature-`, отличие от `errors-`.
- Таблица памяти выросла с двух адресов до четырёх; `README` папки шагов согласован с ней.
- В мастерской (`/code/CLAUDE.md`, правило 25) указаны новое имя и место.

**Решения владельца — дословно.** «Мой выбор один агент при закрытии шага»; «правильно указать название
этого файла во множественном числе внутри указывать как какие файлы связаны когда были начаты и когда
мы можем закрыть»; «нужно написать репорт… когда в конце концов мы закончили эту фичу и последний шаг
этой фичи закрылся»; «абстрагироваться от своего опыта и погрузиться в документацию которую мы
создаем».

**Чего НЕ делать.** Не переносить в проект устройство моих собственных отчётов — владелец сказал прямо,
что их качество не планировалось и опыт оттуда не годится. Не заводить файлы в `reports/` «для
примера»: первый настоящий появится вместе с первой закрытой группой.

**Следующее действие.** Доставить на сервер и показать владельцу форму в деле.

---

## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.

| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · начат · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
