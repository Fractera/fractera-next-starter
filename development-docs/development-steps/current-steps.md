# current-steps.md — состояние проекта между сессиями

## Зачем этот файл существует

**Фича живёт дольше сессии.** От начала разработки до репорта проходит не одна сессия, а иногда
десятки. Каждая новая начинается с пустой головы, и техническое задание шага 6, 8 или 28 ей не
поможет: задание говорит, что задумано, и молчит о том, **что уже сделано и на что смотреть**.

**Поэтому здесь не перечисление шагов, а состояние проекта.** Прочитав только этот файл, агент входит
в разработку сразу — не восстанавливая вчерашний день по коммитам, отчётам и памяти, которой у него
нет.

🔒 **ЭТО НАША ЗАМЕНА `/compact` — И ЗАМЕНА ОБЯЗАТЕЛЬНАЯ** (решение владельца 2026-08-22).
`/compact` сжимает контекст моделью: что именно потеряно, не видит ни агент, ни человек, и пропажа
всплывает через час — забытым решением владельца или законом, который «был в начале сессии».
Здесь состояние записано **буквами, в файл**: его можно прочитать, проверить и поправить, и оно
переживает не только сброс, но и смену модели.

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается. Всё, что уже стало итогом,
отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно:** работа почти никогда не один шаг. Это группа связанных шагов
и подшагов, и половина смысла файла — держать их связи и условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале КАЖДОЙ сессии**, раньше плана шага и раньше кода.

---

## Сейчас в работе

**Группа:** слой данных — право, контракт, навыки, **и доказательство каждой операции** ·
начата 2026-08-22, идёт.

| Шаг | Что | Состояние | Закрывается, когда |
|---|---|---|---|
| 544 | право слоя данных, `/capabilities`, пять навыков | сделано и доставлено | — |
| 545 | **матрица возможностей: каждая операция доказана прогоном** | **64 доказано, 0 провалов, 4 заблокировано (68); код выхода 0** | ключи RAG вписаны владельцем и 4 строки перестали быть блоком |

**Где именно остановились.** `ai-workspace/scripts/probe-capabilities.sh` (коммит есть, доставлен на
сервер, запускается `PUBLIC_DATA_HOST=https://data.aifa.dev bash /opt/fractera/scripts/probe-capabilities.sh`).

**Сделано и чем доказано.**
- Доказаны живьём: вся дверь (включая 401 анонимом из интернета и на неверный секрет), **вся база**
  (создание/чтение/поиск/пагинация/правка/удаление строки/CREATE/ALTER/DROP + `SELECT` с параметрами),
  **все векторы** (запись своим вектором, поиск, фильтр коллекции, удаление обеих записей), **вся
  карта** (геокодинг, маршрут, матрица, порядок объезда), каналы, настройки панели, журнал
  развёртываний. Уборка за собой проверена: временная таблица и записи исчезают.
- Шесть негативных контролей проходят, и каждый отвечает иначе соседа — в том числе **«одинарный
  адрес карты не работает»** (закрепляет находку холодного агента) и «`password` не виден».
- Первый прогон дал три провала — **все три оказались ошибками самой пробы**, не платформы
  (чужой путь к образцу, `page_size` ниже минимума схемы, честный 404 пустого склада иконок).

**ПРОВАЛОВ НЕТ.** Последний (загрузка файла) оказался ЧЕТВЁРТОЙ ошибкой самой пробы против нуля
ошибок платформы: идентификатор лежит в `item`, а извлечение искало его на верхнем уровне. Урок
записан в скрипт: ожидания пишутся по дословному ответу службы, а не по памяти.

**Открытый вопрос уборки.** После прогона в медиатеке остался ОДИН лишний файл `probe-sample.png`
(удалён вручную, контроль показал ноль). Проба удаляет свою загрузку и проверяет `404` у файла — но
это не то же самое, что проверить СПИСОК. Следующий круг обязан проверять список, иначе каждый прогон
будет копить мусор в чужом хранилище.

**Заблокировано ключом (5).** `LLM_BINDING_API_KEY` и `EMBEDDING_BINDING_API_KEY` в
`/opt/fractera/services/rag/.env` — **длина 0**. Без них невозможны загрузка документа, ответ по
документам и удаление документа; плюс запись вектора без своего эмбеддинга и файл иконки (последний
намеренно: генерация оставила бы на сервере значок сайта). Ключ вписывает владелец; после этого тот же
скрипт доказывает строки без единой правки.

**Решение владельца — дословно.** «Во всех пяти навыках все возможные способы чтения записи удаления
работают — только тогда можно говорить, что задача сделана»; «если сейчас ты не гарантируешь хоть на
полпроцента меньше чем 100 процентно гарантию — переделывай, пока не сойдётся».

**Чего НЕ делать.** Не генерировать иконки в пробе (портит значок чужого сайта). Не считать
выборочную проверку доказательством: два вердикта уже были ложными — «адрес карты одинарный» и
«регион не загружен».

**Следующее действие.** Разобрать единственный провал загрузки файла, довести прогон до
`57/62`, затем: зарегистрировать четыре готовых навыка в `CLAUDE.md`
(`use-object-storage`, `use-channels`, `use-products-config`, `use-auth-providers`) и сделать
одобренную хирургию документов (`CONTENT-ENGINE.md` → надгробие, снос `DYNAMIC-WORKFLOWS.md` и
`CASE-TO-STEP.md`).

**Код холодного агента СНЕСЁН ПОЛНОСТЬЮ** (решение владельца 2026-08-23): страница «точки
доставки», четыре двери, `lib/fractera/geo.ts`, пункт меню, таблица в `SCHEMA`, записи в
`check-i18n.mjs`/`TRANSLATION-DEBT.md`/`API-MAP.md` — всё возвращено в исходное состояние. Тестовые
артефакты продукту не нужны; знание прогона сохранено там, где ему место — в навыках (двойной адрес
карты записан в `use-map`).

**Владелец требует ЕЩЁ ОДИН круг:** довести матрицу до 100 % и запустить нового холодного агента на
sonnet. Запуск отложен не по решению агента, а лимитом сессии — это ПЕРВОЕ действие следующей.

---

## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.

| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
