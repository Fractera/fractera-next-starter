# current-steps.md — состояние проекта между сессиями

## Зачем этот файл существует

**Фича живёт дольше сессии.** От начала разработки до репорта проходит не одна сессия, а иногда
десятки. Каждая новая начинается с пустой головы, и техническое задание шага 6, 8 или 28 ей не
поможет: задание говорит, что задумано, и молчит о том, **что уже сделано и на что смотреть**.

**Поэтому здесь не перечисление шагов, а состояние проекта.** Прочитав только этот файл, агент входит
в разработку сразу — не восстанавливая вчерашний день по коммитам, отчётам и памяти, которой у него
нет.

🔒 **ЭТО НАША ЗАМЕНА `/compact` — И ЗАМЕНА ОБЯЗАТЕЛЬНАЯ** (решение владельца 2026-08-22).
`/compact` сжимает контекст моделью: что именно потеряно, не видит ни агент, ни человек, и пропажа
всплывает через час — забытым решением владельца или законом, который «был в начале сессии».
Здесь состояние записано **буквами, в файл**: его можно прочитать, проверить и поправить, и оно
переживает не только сброс, но и смену модели.

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается. Всё, что уже стало итогом,
отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно:** работа почти никогда не один шаг. Это группа связанных шагов
и подшагов, и половина смысла файла — держать их связи и условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале КАЖДОЙ сессии**, раньше плана шага и раньше кода.

---

## Сейчас в работе

**Группа:** Telegram Desk — продукт-паттерн личной эффективности · **построен и доказан живьём**.
План шага — `new-steps/556-telegram-desk-product-tables-orchestration-and-skill.md`.

| Шаг | Что | Состояние |
|---|---|---|
| 556.0–556.5 | мост, таблицы, оркестровка, календарь, поверхность, навык | **сделано** |
| 547–555 | экзамены корпуса инструкций | **приостановлены владельцем**, вопросы не меняются |

## Что работает и чем доказано (всё проверено в живом Telegram)

| Возможность | Доказательство |
|---|---|
| голос слышен | запись `kind:"voice"` с расшифровкой |
| картинка читается | «На изображении…» + текст на ней; `detail:"high"` |
| файлы хранятся | артефакт `media`, файл достаётся обратно целым (`FF D8`) |
| смысл разбирается | `tgdesk_entries`: чек, сумма, продавец |
| три склада разом | артефакты `media` + `vector` + `rag` у одной строки |
| напоминание срабатывает САМО | календарь: строка `done`, сообщение пришло без участия человека |
| часовой пояс | «Канарские острова» → `Atlantic/Canary` записан в `APP-CONFIG` продуктом |
| связка и атрибуция | три сообщения с общим `bundle`, имя из пояснения перешло в сводку |
| перекрёстный поиск | «где покупал компьютер» → Worten; «сколько потратил» → 300 € |

## 🔒 Пять дефектов, найденных за день, — каждый прятался за зелёными проверками

1. **Сквозной прокси слоя данных читал ответ как текст** — двоичное приезжало мёртвым (`FF D8` →
   `EF BF BD`). Найдено дампом файла, а не догадкой.
2. **`\b` не знает кириллицы** — «да» не совпадало никогда, «ok» работало. Пряталось за прежним
   умолчанием «иначе да»: две ошибки компенсировали друг друга.
3. **Умолчанием подтверждения было «да»** — любое сообщение при ожидании молча его подтверждало.
4. **Два поля просили один факт** (`payload.due` против `schedule`) — напоминание тихо становилось
   заметкой.
5. **Слой данных не возвращает `lastInsertRowid`** — код по локальной базе получил бы `NaN`.

🔒 **Урок дня, записанный в навык:** одна инструкция, просящая модель сделать шесть дел разом,
исполняется на пять — молча. Дело, которое нельзя потерять, спрашивают отдельным коротким вызовом.

## Состояние на 2026-08-23, конец дня

**Отправлено всё.** Стартер `7e82397`, платформа `846d785`, оба `main == origin/main`, деревья чистые.
Сервер `109.199.105.213` на платформе `846d785`.

**Тестовые данные снесены полностью:** `tgdesk_*` пусты (0/0/0/0/0), медиа `tg-*` — 0 (остались три
образца стартера), векторы склада `tgdesk` — 0, граф — 0 документов, ящик службы пуст.

**Свежий сервер получит продукт сам.** Служба подставляет адреса дверей и шаг расписания; вручную
остаётся ОДНО — `TELEGRAM_HOOK_SECRET` в `.env.local` слота. Нет секрета — бот честно работает
по-старому, отвечая из базы знаний.

## Долги, названные вслух

| Долг | Почему не сделано |
|---|---|
| PDF не читается | библиотеки нет, заводить зависимость — решение владельца; файл сохраняется |
| видео | отложено владельцем: частный случай, разбор дорогой |
| репорт фичи | группа 556 не закрыта репортом `feature-` |
| экзамены 547–555 | ждут слова владельца |
| поле пояса в панели | продукт пишет пояс сам; формы в панели для него нет |

**Следующее действие.** Слово владельца: развернуть новый сервер, вернуться к экзаменам или закрыть
группу репортом фичи.

---

## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.

| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
