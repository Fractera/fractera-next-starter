# current-steps.md — состояние проекта между сессиями

## Зачем этот файл существует

**Фича живёт дольше сессии.** От начала разработки до репорта проходит не одна сессия, а иногда
десятки. Каждая новая начинается с пустой головы, и техническое задание шага 6, 8 или 28 ей не
поможет: задание говорит, что задумано, и молчит о том, **что уже сделано и на что смотреть**.

**Поэтому здесь не перечисление шагов, а состояние проекта.** Прочитав только этот файл, агент входит
в разработку сразу — не восстанавливая вчерашний день по коммитам, отчётам и памяти, которой у него
нет.

🔒 **ЭТО НАША ЗАМЕНА `/compact` — И ЗАМЕНА ОБЯЗАТЕЛЬНАЯ** (решение владельца 2026-08-22).
`/compact` сжимает контекст моделью: что именно потеряно, не видит ни агент, ни человек, и пропажа
всплывает через час — забытым решением владельца или законом, который «был в начале сессии».
Здесь состояние записано **буквами, в файл**: его можно прочитать, проверить и поправить, и оно
переживает не только сброс, но и смену модели.

**Один файл на весь проект.** Переписывается ПОВЕРХ, а не дописывается. Всё, что уже стало итогом,
отсюда уходит в `completed-steps/`.

**Имя во множественном числе намеренно:** работа почти никогда не один шаг. Это группа связанных шагов
и подшагов, и половина смысла файла — держать их связи и условия закрытия, которых нет больше нигде.

**Читается ПЕРВЫМ в начале КАЖДОЙ сессии**, раньше плана шага и раньше кода.

---

## Сейчас в работе

**Группа:** слой данных — право, контракт, навыки, **доказательство каждой операции** · идёт.

| Шаг | Что | Состояние | Закрывается, когда |
|---|---|---|---|
| 544 | право слоя данных, /capabilities, пять навыков | сделано и доставлено | — |
| 545 | **матрица возможностей** | 78/78, провалов 0, блоков 0; два прогона подряд дали то же; **приёмка холодным агентом пройдена 2026-08-23** | закрыт |
| 546 | инструкция в соответствие: регистрация навыков, вердикт, хирургия, пересборка `CLAUDE.md` | всё сделано; `CLAUDE.md` 1375 → 856 строк, по-английски | закрыт |

**Как запустить доказательство (одна команда):**
`PUBLIC_DATA_HOST=https://data.aifa.dev bash /opt/fractera/scripts/probe-capabilities.sh`

**Доказано живьём, 78 операций.** Вся дверь · вся база (создание, чтение, поиск, пагинация, правка,
удаление строки, CREATE/ALTER/DROP, SELECT с параметрами) · вся медиатека (загрузка, файл, превью,
правка, удаление, **весь путь иконок**) · все векторы (свой вектор И расчёт моделью, поиск словами,
удаление) · **весь граф знаний** (загрузка документа, ответ по нему, удаление с проверкой списка) ·
вся карта · каналы · настройки панели · журнал развёртываний. Семь негативных контролей.

**Уборка проверена по КАЖДОМУ складу после ДВУХ прогонов:** медиа 0, наборов иконок 0, векторов 0,
временных таблиц 0, документов 0. Сайт 200, панель отвечает, значок сайта на месте.

**Найдено и исправлено в ПРОДУКТЕ (не в проверке):** у платформы не было удаления набора иконок —
создать значок сайта было можно, отменить нельзя. Добавлен `DELETE /media/icons/:id`, файлы и запись
уходят вместе (`ec0add4`).

**Пять ошибок проверки против нуля ошибок платформы.** Все провалы всех кругов были моими: чужой путь
к образцу, page_size ниже минимума схемы, честный 404 пустого склада, id в `item`, удаление документа
по имени вместо id при асинхронном удалении. Урок записан в скрипт: **ожидание пишется по дословному
ответу службы, а проверяется РЕЗУЛЬТАТ, а не код ответа**.

**Решение владельца — дословно.** «Во всех пяти навыках все возможные способы чтения записи удаления
работают — только тогда можно говорить, что задача сделана»; «мне не нужны твои временные файлы».

**Чего НЕ делать.** Не оставлять после проверки ничего в чужих складах. Не считать код 200
доказательством: граф отвечает `deletion_started` и работает в фоне.

**Приёмка холодным агентом пройдена (два круга, 2026-08-23).** Агент строил склад запчастей: три
таблицы, четыре двери, две страницы, семантический поиск с сортировкой складов по реальному
расстоянию; 21 гейт зелёный, артефакты снесены той же операцией (дерево чистое, `17cdb97`). Первый
круг сломался на карте — двойной адрес службы; после правки навыка второй прошёл её чисто.

**Две находки вошли в `use-map` живыми числами:** соседние маршруты отвечают в РАЗНЫХ единицах
(`route` — километры и минуты, `matrix` для той же пары — метры и секунды: `4.29`/`10.5` против
`4303.9`/`632.1`), и матрица НЕСИММЕТРИЧНА (`4303.9` туда против `4039.4` обратно). Прочитать ячейку
матрицы как километры — ошибиться в тысячу раз числом, которое на экране доставки выглядит
правдоподобно. Там же `use-vector-memory` теперь сам говорит, что его помощник в проекте ни разу не
вызывался (ноль импортов).

**Инструкция приведена в соответствие (2026-08-23).** Зарегистрированы четыре навыка —
`use-object-storage`, `use-channels`, `use-products-config`, `use-auth-providers`. Вынесен вердикт по
шести планам: `use-troubleshooting`, `use-passport`, `use-panel` отменены (имена убраны и из таблицы
навыков, и из таблицы портов), `use-dynamic-workflows` и `use-use-cases` — писать,
`use-migration` — план с условием «сначала пройти два первых шага очереди на чужом проекте».

**Хирургия трёх документов закончена 2026-08-23 — переездом, а не удалением.** `CONTENT-ENGINE.md`
(584 строки) разошёлся по семи навыкам; `DYNAMIC-WORKFLOWS.md` (145) стал навыком
`use-dynamic-workflows`; `CASE-TO-STEP.md` (92) стал `use-use-cases`. На месте каждого — надгробие с
картой переезда: **удаление в `development-docs/` — решение владельца, а не агента** (закон их
`README.md`), поэтому файлы лежат пустыми оболочками и ждут его слова.

🔒 **Хирургия нашла две структурные неправды в `CASE-TO-STEP.md`** — по ним строили бы: он объявлял
три поверхности закона (MCP `fractera-project` и навык `manage-cases-and-steps` не существуют вовсе)
и селил кейсы в папке `USE-CASES/`, тогда как они живут в досье продукта `PRODUCTS-CONFIG/<id>.json`,
массив `cases[]`. Та же ошибка про папку успела попасть в свежий `use-dynamic-workflows` — исправлена
в тот же заход.

**`CLAUDE.md` пересобран 2026-08-23 — 1375 → 856 строк, целиком по-английски.** Не перевод:
переопределение. «Кто ты» стал первым блоком, доверие и граница выдумки вошли внутрь него; вместо
прозы «чем оплачено» введена нотация `🔒 закон · ✗ дефект · → навык · 🪦 отменено`.

**Сверка по реестру законов:** было 58 под 🔒, стало 52 — и каждая разница названа: два закона про
`/compact` слиты в один; три уехали в `BACKLOG.md` (чего НЕ доказывает `use-pwa`, `use-code-shape`
не проверен стройкой, воркер); два — в навыки (`use-map`, `use-static-pages`); вердикт по шести
планам стал таблицей `Cancelled`; добавлен один новый (свобода = пространство архитектуры).
Непристроенных нет.

🔒 **Проверка путей нашла третью выдумку за день:** `PAGES.md` не существует — план страниц живёт
массивом `pages` в досье продукта. Там же исправлены `registry.json` (создаётся в рантайме) и число
гейтов: восемнадцать в `prebuild`, а не пятнадцать.

**`app/AGENTS.md` переписан:** он описывал `app/[lang]/legal/`, `app/api/legal/`, `app/(service)/`
и `app/[lang]/page.tsx` — ни одного из них нет; ссылался на несуществующий раздел «You may have EYES»
и на `ARCHITECTURE.md`, которого в проекте нет.

**Следующее действие.** Группа 544–546 закрыта; итоги в `completed-steps/`, репорт фичи — по слову
владельца.

---

## Форма записи

Разделы выше — и есть форма; пустые не удалять, пустой раздел это тоже ответ.

| Раздел | Что в нём |
|---|---|
| **Группа** | одной фразой, что строим целиком, и когда начали |
| **Таблица шагов** | номер · что · состояние · **условие закрытия** |
| **Попутные шаги** | те, которых не планировали, — с причиной появления |
| **Условие закрытия группы** | что должно быть верно, чтобы вся группа считалась законченной |
| **Сделано и чем доказано** | по строке на пункт: не «сделал форму», а «форма записана, хэши сошлись» |
| **Решения владельца** | дословно, не пересказ: пересказ теряет то, ради чего решение принято |
| **Чего НЕ делать** | тупики, уже оплаченные в этой группе |
| **Следующее действие** | одна фраза; список — это план, он живёт в файле шага |

---

## Когда этот файл обнуляется

**Агент обнуляет его сам, при закрытии группы, той же операцией.** Содержимое уходит двумя адресами:
итоги шагов — в `completed-steps/`, а картина целого — в **репорт фичи** (`development-docs/reports/`,
категория `feature-`). После этого здесь снова «пусто — работа не начата».

Файл, который не обнуляют, за месяц становится вторым журналом: его чтение само превращается в расход
контекста — ровно в тот, ради экономии которого он заведён.
